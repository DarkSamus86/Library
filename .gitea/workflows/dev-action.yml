name: Dev deploy to Kubernetes
on:
  push:
    branches: [dev]
jobs:
  build-and-deploy:
    runs-on: host
    steps:
      - name: Checkout code
        run: |
          git config --global http.sslVerify false
          git clone https://ro8otov:${{ secrets.runner_token }}@gitea.local/darksamus/Library.git .
          git checkout ${{ github.sha }}
      - name: Build and push Docker image
        run: |
          docker build -t registry.robotov.site/dev-library:1.0.3 .
          docker push registry.robotov.site/dev-library:1.0.3
      - name: Deploy to Kubernetes
        run: |
          kubectl apply -f k8s/ -n dev
          kubectl rollout status deployment/dev-library -n dev --timeout=180s

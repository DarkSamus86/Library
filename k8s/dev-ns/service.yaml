apiVersion: v1
kind: Service
metadata:
  name: dev-library-service
  namespace: dev
spec:
  type: ClusterIP
  selector:
    app: dev-library
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
apiVersion: v1
kind: Service
metadata:
  name: dev-library-service
  namespace: dev
spec:
  type: NodePort
  selector:
    app: dev-library
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
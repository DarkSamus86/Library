spring:
  datasource:
    url: // url
    username: // username
    password: // password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
logging:
  file:
    name: /var/log/library-app/library-app.log